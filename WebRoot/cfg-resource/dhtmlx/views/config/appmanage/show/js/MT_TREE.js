/**
 * MT => module template 模块模板 TREE => tree 树
 * @param that 全局变量
 * @param tLayout 树布局面板
 * @param rLayout 树节点布局面板
 */
function MT_TREE_init(that, tLayout, nLayout) {
    MT_BASE_TREE_init.call(this, that, tLayout, nLayout);
}