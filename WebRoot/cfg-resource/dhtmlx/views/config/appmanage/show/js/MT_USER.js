/**
 * MT => module template 模块模板 TREE => tree
 * @param tLayout 树布局面板
 * @param rLayout 树节点布局面板
 */
function MT_USER_init(tLayout, clickEvent, showLoginName) {
    MT_BASE_USER_init.call(this, tLayout, clickEvent, showLoginName);
}